import type { NextConfig } from "next";

const nextConfig: NextConfig = {
    // други опции, ако имаш, но без output: 'export'
    reactStrictMode: true,
    // ... други настройки по желание
};

export default nextConfig;
